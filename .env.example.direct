# AWS Bedrock Direct Connection Configuration
# This configuration is for bedrock_mcp_agent.py (direct connection to AWS Bedrock)

# AWS Bedrock Configuration
BEDROCK_MODEL_ID=anthropic.claude-3-5-sonnet-20241022-v2:0
AWS_REGION=us-east-1
BEDROCK_MAX_TOKENS=4096
BEDROCK_TEMPERATURE=0.0

# AWS Credentials (choose one method)
# Method 1: Environment variables (uncomment to use)
# AWS_ACCESS_KEY_ID=your_access_key_id
# AWS_SECRET_ACCESS_KEY=your_secret_access_key

# Method 2: AWS CLI (~/.aws/credentials) - recommended
# Run: aws configure

# Method 3: IAM Role (if running on EC2/ECS)
# No configuration needed

# MCP Server Configuration
MCP_SERVER_PATH=/Users/henryshen/Documents/Cline/MCP/zscaler-mcp-server/.venv/bin/python
MCP_SERVER_MODULE=zscaler_mcp.server

# Zscaler API Credentials (for MCP server)
ZPA_CLIENT_ID=your_zpa_client_id
ZPA_CLIENT_SECRET=your_zpa_client_secret
ZPA_CUSTOMER_ID=your_zpa_customer_id
ZPA_CLOUD=your_zpa_cloud

ZIA_USERNAME=your_zia_username
ZIA_PASSWORD=your_zia_password
ZIA_API_KEY=your_zia_api_key
ZIA_CLOUD=your_zia_cloud
